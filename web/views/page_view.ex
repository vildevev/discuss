defmodule Discuss.PageView do
  use Discuss.Web, :view
  # Has an 'index.html' file in template folder that's in folder with the same name "Page" in this case.
  # Naming conventions are important.
  # Debug by running: 'iex -S mix phoenix.server' ex: 'Discuss.PageView.render("index.html")'
end
